function res = multi_modal_gaussian_target(x)

res = mixture_gaussian_target( ...
    x, ...
    [0.1, 0.2, 0.3, 0.2, 0.2], ...
    [-10; -5; -2; 2; 6], ...
    cat(3, eye(1), eye(1), eye(1), eye(1), eye(1)) ...
    );

end